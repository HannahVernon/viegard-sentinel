<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["Viegard.AdminApi.styles.css"]" />
    <HeadOutlet />
</head>

<body>
    @* Every admin page is static SSR with plain HTML form posts; the app
       deliberately ships no client-side Blazor script.  blazor.web.js and
       the reconnect modal caused a spurious "unhandled error" banner on
       anonymous pages and are gone until an increment actually needs
       interactivity. *@
    <Routes />
</body>

</html>
